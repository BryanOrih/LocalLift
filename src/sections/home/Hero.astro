---
import HeroIconBg from "@svg/heroIconBg.svg";
import HeroImage from "@images/Home/Hero/hero-image.webp";

import { Image } from "astro:assets";
---

<section class="hero-section" id="Hero-section">
  <HeroIconBg class="hero-bg" />
  <div class="hero-content">
    <h1>Websites That Get Local Businesses More Clients</h1>

    <p>
      We design high-converting websites and optimize your online presence so
      customers can find you, trust you, and book with you.
    </p>
    <button>Free Consultation</button>
  </div>

  <Image
    class="hero-icon"
    src={HeroImage}
    alt="Hero Image"
    format="webp"
    quality={70}
  />
</section>

<style>
  .hero-section {
    background-color: #1e1e1e;
    padding: min(80px, 10vw) 0 min(80px, 10vw) 0;
    align-items: center;
    position: relative;
    display: flex;
    flex-direction: column;
    justify-content: space-evenly;
  }

  .hero-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
  }

  .hero-icon {
    height: fit-content;
    width: 45%;
    position: relative;
    max-width: 800px;
    display: none;
  }

  .hero-content {
    display: flex;
    padding: 40px;
    flex-direction: column;
    max-width: 600px;
    gap: 40px;
    z-index: 2;
    & h1,
    p {
      color: white;
    }
    & h1 {
      font-weight: 700;
      font-size: var(--step-5);
      font-weight: 700;
    }
    & button {
      background-color: #1777eb;
      color: white;
    }
  }

  @media (min-width: 1280px) {
    .hero-section {
      flex-direction: row;
    }

    .hero-content {
      width: 45%;
      padding: 0px;
    }

    .hero-icon {
      display: block;
    }
  }
</style>
